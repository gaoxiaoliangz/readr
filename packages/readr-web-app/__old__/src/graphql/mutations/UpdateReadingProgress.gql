mutation updateReadingProgress($bookId: ID!, $percentage: Float!) {
  updateReadingProgress(input: { bookId: $bookId, percentage: $percentage }) {
    ok
    n
    nModified
    clientMutationId
  }
}
