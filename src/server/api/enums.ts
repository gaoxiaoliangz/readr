export enum PostVisibility {
  Private = 0,
  Public = 1
}

export enum PostStatus {
  Draft = 0,
  Published = 1,
  Unpublished = 2
}

export enum PostCategories {
  Blog = 0,
  BookWeekly = 1,
  Pages = 2
}
