#import './BookRecursiveElements.gql'

fragment BookPages on BookPageConnection {
  totalCount
  fromHistory
  fromLocation
  startPage
  offset
  edges {
    cursor
    node {
      meta {
        pageNo
        offset
      }
      elements {
        ...BookRecursiveElements
      }
    }
  }
}
